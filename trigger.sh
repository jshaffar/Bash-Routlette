#!/bin/bash
echo "warning: you can still back out by pressing n"
echo "What mode would you like to play???. Type h for history mode (goes through your bash history and picks random command), c for custom mode (has 1 in x chance of running command), d for death mode (has 1 in x chance of wiping disk) s for shutdown mode (calls a shutdown now), and n to exit"
read input
echo $input mode it is
